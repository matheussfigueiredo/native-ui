import { View } from "react-native";
import { Typography } from "@/components/ui/typography";

export default function Index() {
  return (
    <View style={{ flex: 1, padding: 20, gap: 10 }}>
      <Typography>Native UI - Run</Typography>
    </View>
  );
}
